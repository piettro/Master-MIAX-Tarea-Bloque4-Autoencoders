[flake8]
max-line-length = 79
exclude = .venv,venv,__pycache__,professor_solution,materials,notebooks,build,dist

[tool:pytest]
testpaths = tests
pythonpath = .
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
